"""
Pasos a seguir para ejecutar el codigo en el raspberry 
"""


# Actualizar sistema
sudo apt update && sudo apt upgrade -y

# Instalar pip si no está
sudo apt install python3-pip -y

# En la Raspberry Pi (clonar el repo)
clonar el repo
dirigirse a la carptea de Raspbeery_code

# Instalar dependencias
1. pip3 install -r requirements.txt
2. pip3 install adafruit-circuitpython-dht RPi.GPIO rpi-lcd board  // no se cual comando vaya a funcionar xd

# Ejecutar Con privilegios (si usa GPIO)
sudo python3 main.py

# instalar MQTT 
pip install paho-mqtt

# instalar rpi 
pip install rpi_lcd
